{
  "compilerOptions": {
    "outDir": "dist", // 빌드 파일 지정
    "rootDir": "src", // root 디렉토리
    "lib": ["esnext"],
    "sourceMap": false, // 소스맵 생성 안함
    "watch": false, // 빌드 파일 생성 여부
    "strict": true // # 엄격모드
  },
  "include": [ // 컴파일 대상
    "src/**/*"
  ],
  "exclude":  [ // 컴파일 대상 제외 리스트
		"node_modules"
	]
}